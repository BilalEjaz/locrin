[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "locrin"
version = "0.0.1"
description = "Locrin: the deterministic quality gate for code written by people and agents. Placeholder release; the engine ships here."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"

[project.urls]
Homepage = "https://locrin.com"

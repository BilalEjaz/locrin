// A test file that declares no cases at all: setup shared by the suites that
// import it. Nothing to say about it.
export function makeRow(id: number) {
  return { id };
}

export const emptyList = { count: 0 };

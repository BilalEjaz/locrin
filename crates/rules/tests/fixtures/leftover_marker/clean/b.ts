// TODO(#123) handle the empty case
export function f(): number {
  return 1; // FIXME FL-42 wrong for negatives
}
// see https://example.com/todo for context, TODO tracked there
// todo in lowercase is prose, not a marker
// TODO drop the shim once the flag ships // locrin:allow
// see avatars/XXX.jpg for the placeholder the uploader writes
// the id has the shape XXX-XXX-XXXX
// fixtures/TODO_list.json is the seed file

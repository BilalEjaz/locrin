export function keep(): number {
  // The old path summed the rows twice, so it is parked here for now.
  // function oldTotal(rows: number[]): number {
  //   let running = 0;
  //   for (const row of rows) {
  //     running += row;
  //   }
  //   return running;
  // }
  return 1;
}
